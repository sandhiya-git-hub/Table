#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    int size;

    printf("Enter array size (for PxP array): ");

    scanf("%d", &size);

    printf("Array size entered: %d\n", size);

    return 0;
}